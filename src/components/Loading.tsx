const Loading = () => {
	return (
		<div className="z-30 flex h-screen flex-col items-center justify-center text-center font-almamono text-7xl text-white">
			<h2 className="z-30 animate-pulse text-3xl">Turning on the lights...</h2>
		</div>
	);
};

export default Loading;
